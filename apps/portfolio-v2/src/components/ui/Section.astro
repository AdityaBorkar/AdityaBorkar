---
interface Props {
	id?: string;
	class?: string;
}

const { class: className, id } = Astro.props;
---

<section
	id={id}
	class:list={["max-w-[64rem] mx-auto", "max-md:px-4", className]}
>
	<slot />
</section>
