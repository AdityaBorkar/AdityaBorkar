---
import Chat from '@/components/chat/index.tsx';
import RootLayout from '@/layouts/RootLayout.astro';
import { SchemaMarkup } from '@/lib/utils';

const title = 'Chat with Aditya Borkar';
const description = 'Chat with Aditya Borkar';
const keywords = ['chat', 'aditya borkar', 'aditya', 'borkar'];
const schema_markup = [SchemaMarkup.get('webpage/page')];
const open_graph = {};
const seo = { title, description, keywords, schema_markup, open_graph };
---

<RootLayout {...seo}>
    <Chat client:only="react" />
</RootLayout>
<!--
<script>
    import Lenis from "lenis";

    setTimeout(() => {
        const wrapper = document.getElementById("chat-window");
        console.log({ ok: wrapper?.querySelector("main") });
        if (!wrapper) return;
        const modalLenis = new Lenis({
            autoRaf: true,
            wrapper,
            content: wrapper.querySelector("main"),
        });
    }, 1000);
</script> -->
