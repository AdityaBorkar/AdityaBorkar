---
import { Image } from "astro:assets";
import { getImage } from "@/lib/assets";

type Props = {
    class?: string;
    id?: string;
};

const { class: className, id } = Astro.props;
---

<section class:list={["px-4 md:px-0 max-w-[64rem] mx-auto", className]} id={id}>
    <slot />
</section>
