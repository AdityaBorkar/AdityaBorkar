---
// const BlogPosts = await getCollection("blogs");

const Tags = [
	// TODO - collect tags from all posts
	'🌐 All',
	'💻 JavaScript / TypeScript',
	'🔍 Dev Tooling',
	'💡 Webflow',
	'🚀 Cloudflare',
	'💡 React.js',
];
---

<!-- <LandingLayout
  title="Blog"
  description="Blog"
  keywords={["Blog"]}
  schema_markup={SchemaMarkup}
> -->
<div class="mx-auto max-w-[80rem]">
  <h1 class="mb-8 mt-36 text-5xl font-bold">Blog Posts</h1>
  <div class="mb-16 text-base text-text-secondary">
    Notes and essays I write in my digital garden.
  </div>

  <div class="mb-12 flex items-center justify-between">
    <div class="flex gap-2">
      {
        Tags.map((tag) => (
          <span class="rounded-full border border-border/20 px-3 py-1 text-sm font-medium">
            {tag}
          </span>
        ))
      }
    </div>
    <div>Search</div>
  </div>

  <div class="mb-16 grid grid-cols-3 gap-10">
    <!-- {
      BlogPosts.map(({ id, data: post }) => (
        <a href={`/blog/${id}`} class="transition-all duration-500">
          {/* HOVER: SHINE EFFECT */}
          <img
            src={post.image}
            alt={post.title}
            class="w-full min-h-[14rem] rounded-xl border border-border/20 object-cover"
          />
          <div class="mt-2 px-4 py-1 text-lg font-medium leading-tight">
            {post.title}
          </div>
        </a>
      ))
    } -->
  </div>
</div>
<!-- </LandingLayout> -->
